apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "waffle-sizzler.fullname" . }}-test-connection"
  labels:
    {{- include "waffle-sizzler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "waffle-sizzler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
