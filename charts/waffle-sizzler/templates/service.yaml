apiVersion: v1
kind: Service
metadata:
  name: {{ include "waffle-sizzler.fullname" . }}
  labels:
    {{- include "waffle-sizzler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "waffle-sizzler.selectorLabels" . | nindent 4 }}
