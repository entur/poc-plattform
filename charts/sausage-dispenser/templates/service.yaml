apiVersion: v1
kind: Service
metadata:
  name: {{ include "sausage-dispenser.fullname" . }}
  labels:
    {{- include "sausage-dispenser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sausage-dispenser.selectorLabels" . | nindent 4 }}
