apiVersion: v1
kind: Service
metadata:
  name: {{ include "machine-unlearner.fullname" . }}
  labels:
    {{- include "machine-unlearner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "machine-unlearner.selectorLabels" . | nindent 4 }}
