apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "machine-unlearner.fullname" . }}-test-connection"
  labels:
    {{- include "machine-unlearner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "machine-unlearner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
